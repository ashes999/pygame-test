# PyGame Test

Just checking that PyGame is awesome.

- To run the game: `python main.py`
- To build a self-contained executable for *nix: `./build.sh`
- To build a self-contained executable for Windows: Fork, copy and modify `build.sh` into a batch file, commit, and open a PR.
